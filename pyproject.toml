[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "carryoncarlos"
version = "0.1.0"
dependencies = [
    "google-cloud-logging",
    "fastapi",
    "uvicorn",
    "openai",
    "python-dotenv"
]

[tool.setuptools.packages.find]
include = ["app"]
